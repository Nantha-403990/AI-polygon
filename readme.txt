#1 process_csv if processed_apartment_graphs.pt is not there. 
#2 train_vgae
#3 visualize_embedding

# Merged Partitions (Ignored)

This directory contains CSV partitions generated from the Swiss Dwellings dataset.

Due to size (~2GB), this directory is excluded from Git tracking via `.gitignore`.

To regenerate the data, run:
```bash